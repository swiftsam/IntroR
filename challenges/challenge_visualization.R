### CHALLENGE: visualization
### Task:  Make a plot using a new dataset using at least 3 layers
### Bonus: Use two geoms we haven't used yet
### http://docs.ggplot2.org/current/

data(mtcars)
head(mtcars)
